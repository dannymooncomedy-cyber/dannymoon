Place your audio files here using these exact filenames so the site references work:

- DannyMoon_VoiceReel.wav    → appears as “Voice Reel” on the home page
- Spirits_Audition.wav       → appears as “Monster” in Voice Samples
- Snickers_DannyMoon.wav     → appears as “Snickers Commercial” in Voice Samples
- Narrator_DannyMoon.wav     → appears as “Documentary” in Voice Samples

If you prefer MP3, update the file extensions in `index.html` and `script.js` accordingly.

To use the header image you mentioned, copy it to `media/images/header.jpg` (replace the existing file). The site will use the header image and color variables defined in `style.css`.

After copying files, open the site in a browser to test playback and contact form submissions.
